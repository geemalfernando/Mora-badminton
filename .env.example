# ============== Backend (umisf-server) – for Vercel / Render ==============
# Add these in Vercel: Project → Settings → Environment Variables (or Render: Environment)

# ----- Required -----
# MongoDB connection string (Atlas: Network Access allow 0.0.0.0/0 or add Vercel/Render IPs)
DATABASE_URL=mongodb+srv://<user>:<password>@<cluster>/UMISF?retryWrites=true&w=majority
DB_NAME=UMISF

# ----- Optional (backwards-compatible) -----
CONNECTION_STRING=
DATABASE=

# ----- API -----
API_PREFIX=/api

# ----- Optional: Cloudinary (images; if unset, stored as data URLs in MongoDB) -----
CLOUD_NAME=
CLOUD_KEY=
CLOUD_SECRET=

# ----- Optional: admin API protection -----
ADMIN_API_KEY=

# ----- Server (Vercel sets PORT automatically; use for local or Render) -----
PORT=3001
